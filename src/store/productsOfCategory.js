import { createSlice } from "@reduxjs/toolkit";

const productsOfCategory = createSlice({
    name: 'products',
    initialState: [],
    reducers: {
        
    }
});

export default productsOfCategory;